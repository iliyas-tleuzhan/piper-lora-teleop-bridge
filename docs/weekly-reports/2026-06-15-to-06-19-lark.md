# 2026.6.15-6.19 Weekly Report

| Description / Week | Original Plan | Summary and Improvements for Solutions (What was done) | Difficulties (What remains unresolved and challenges encountered) | Reflection | Plan Next Week | Related Development Document |
| --- | --- | --- | --- | --- | --- | --- |
| Piper LoRa master-slave teleoperation and operation documentation | 1. Port the working Piper UDP teleoperation behavior to LoRa through two ESP32 boards. <br>2. Make the slave arm follow the master arm with low delay and without startup jumps. <br>3. Document the real operating procedure for both arms. | 1. Implemented real binary LoRa teleoperation packets instead of fake tests. <br>2. Computer 1 now reads Piper CAN target frames and sends compact `PLT1` packets through Board A. <br>3. Computer 2 now validates Board B packets and commands the slave Piper through `piper_sdk`. <br>4. Added startup protection so the first packet holds the current slave pose instead of jumping to an old target. <br>5. Improved speed by using a 50 Hz sender request with Board A `TX done` flow control. <br>6. Improved accuracy by changing the receiver from relative-offset tracking to absolute master joint target tracking. <br>7. Added scripts to read/apply matching Piper motor limits and to perform guided zero calibration. <br>8. Updated GitHub README and added a short operation guide. | 1. The master arm can expose different CAN sources depending on setup: feedback frames `0x2A5/0x2A6/0x2A7` or command frames `0x155/0x156/0x157`. This made early debugging confusing. <br>2. LoRa has much lower bandwidth than UDP, so packet size, send rate, and radio flow control had to be optimized. <br>3. Startup safety and exact following conflicted at first: using a relative baseline avoided jumps but created position mismatch. This was fixed by holding only the first packet and then using absolute tracking. <br>4. Some remaining inaccuracy may come from different firmware motor limits or different joint zero calibration between the two physical arms. <br>5. Direct Lark publishing is not available from this Codex environment, so this Markdown version is prepared for Lark upload/paste. | 1. Test robot systems in layers: CAN decoding, serial, LoRa, receiver validation, and small slave movement before full teleoperation. <br>2. Persistent robot settings such as motor limits and zero calibration should be checked before blaming the wireless link. <br>3. Startup procedures must be conservative because stale command frames can move the slave unexpectedly. <br>4. Documentation should include exact commands, expected logs, and stop conditions so the system can be reset and reproduced safely. | 1. Pull the latest repo on both computers. <br>2. Read the current motor limits on both arms with `piper_configure_motor_limits.py`. <br>3. Apply the same full-motion profile to both arms only if their firmware limits differ. <br>4. If static mismatch remains, perform guided zero calibration using the same physical zero reference on both arms. <br>5. Record Computer 1 `source=feedback` or `source=command` during tests and compare joint feedback from both arms at several poses. <br>6. Continue testing in a small safe workspace before increasing motion range. | GitHub repo README: `README.md` <br>Operation guide: `docs/operation.md` <br>Piper SDK interface docs: https://github.com/agilexrobotics/piper_sdk/blob/master/asserts/V2/INTERFACE_V2.MD <br>Piper SDK motor-limit demo: https://github.com/agilexrobotics/piper_sdk/blob/master/piper_sdk/demo/V2/piper_set_motor_angle_limit.py <br>Piper SDK zero-calibration demo: https://github.com/agilexrobotics/piper_sdk/blob/master/piper_sdk/demo/V2/piper_set_joint_zero.py |

## Notes For Lark

Paste the table above into the Lark weekly report page for `2026.6.15-6.19`. If Lark does not preserve the Markdown table, paste each column into the existing weekly-report table:

- Description / Week
- Original Plan
- Summary and Improvements for Solutions
- Difficulties
- Reflection
- Plan Next Week
- Related Development Document
